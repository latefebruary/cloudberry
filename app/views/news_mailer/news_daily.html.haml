!!!
%html
  %head
    %meta{ :content => 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type' }

  %body

    %h3
      Новое за неделю 

    %p
    - @user.subscriptions.each do |sub|
      - Category.find(sub.category_id).articles.created_after(1.week.ago).each do |article|
        .lightgrayline
        
        %p
          %h4= article.title
        %p
          = article.anonce
        %p
          = article.updated_at
        %p
          - Category.find(article.category_ids).each do |cat|
            = cat.name
        %p
          = link_to 'read', article_url(article)


    %p
      Чтобы отписаться от рассылки, нажмите
      = link_to 'Отписаться', unsubscribe_url( user_token: @user.user_token )
